"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findPackageJson = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = require("path");
function findPackageJson(cwd, cache = new Map(), visited = []) {
    while (cwd) {
        if (cache.has(cwd)) {
            return {
                packageJsonPath: cache.get(cwd),
                visited,
                cache,
            };
        }
        const packageJsonPath = (0, path_1.join)(cwd, 'package.json');
        visited.push(cwd);
        if (fs_1.default.existsSync(packageJsonPath)) {
            for (const dir of visited) {
                cache.set(dir, packageJsonPath);
            }
            return {
                packageJsonPath,
                visited,
                cache,
            };
        }
        else {
            const parent = (0, path_1.dirname)(cwd);
            if (parent === cwd) {
                return null;
            }
            cwd = parent;
        }
    }
    return null;
}
exports.findPackageJson = findPackageJson;
//# sourceMappingURL=find-package-json.js.map