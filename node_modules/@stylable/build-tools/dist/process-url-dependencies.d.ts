import type { StylableMeta } from '@stylable/core';
export declare function processUrlDependencies({ meta, rootContext, filter, getReplacement, host, }: {
    meta: {
        source: StylableMeta['source'];
        targetAst?: StylableMeta['targetAst'];
    };
    rootContext: string;
    filter?: (url: string, context: string) => boolean;
    getReplacement?: (params: {
        urls: string[];
        url: string;
        rootContext: string;
        importerDir: string;
        absoluteRequest: string;
        index: number;
    }) => string;
    host: {
        join: (...paths: string[]) => string;
        isAbsolute: (path: string) => boolean;
    };
}): string[];
//# sourceMappingURL=process-url-dependencies.d.ts.map