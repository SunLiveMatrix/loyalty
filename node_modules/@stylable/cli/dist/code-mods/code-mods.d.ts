import { FileSystem } from '@stylable/node';
import type { CodeMod } from './types';
import type { Log } from '../logger';
export interface CodeModsOptions {
    fs: FileSystem;
    rootDir: string;
    extension: string;
    mods: Set<{
        id: string;
        apply: CodeMod;
    }>;
    log: Log;
}
export declare function codeMods({ fs, rootDir, extension, mods, log }: CodeModsOptions): void;
//# sourceMappingURL=code-mods.d.ts.map